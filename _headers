# Cloudflare Pages / Netlify header rules.
#
# Pages' default for static assets is `public, max-age=14400` — four hours
# during which a browser serves its stored copy WITHOUT revalidating. None of
# the scripts below are content-hashed or version-queried, so a deploy stayed
# invisible in any tab that had already fetched them (this is exactly how a
# city-map.js fix shipped and could not be seen without a hard reload).
#
# index.html is already `max-age=0, must-revalidate` by default, so the page
# itself is always current; the rules here bring its local scripts and styles
# in line. Revalidation costs one conditional request each and returns a 304
# when nothing changed — negligible for four small files, and it means a
# deploy is visible immediately.

# Listed one by one on purpose. `_headers` only supports a TRAILING wildcard —
# `/assets/*` below matches, but `/*.js` and `/*.css` never do, so the rules
# that used to be here were dead and Pages kept serving these at its four-hour
# default. That is the exact failure this file was added to prevent, and it
# resurfaced when a hero copy change shipped alongside the CSS that lays it
# out: index.html is always fresh, so returning visitors got new markup against
# a stale stylesheet. Verify with `curl -sI .../site.css | grep -i cache` after
# a deploy, and add a line here whenever a script or stylesheet is added.

/site.css
  Cache-Control: public, max-age=0, must-revalidate

/city-map.js
  Cache-Control: public, max-age=0, must-revalidate

/site-screens.js
  Cache-Control: public, max-age=0, must-revalidate

/waitlist.js
  Cache-Control: public, max-age=0, must-revalidate

/tweaks-panel.jsx
  Cache-Control: public, max-age=0, must-revalidate

/site-tweaks.jsx
  Cache-Control: public, max-age=0, must-revalidate

# Images are the large files and change rarely — cache them hard. Any image
# that does change should be renamed rather than replaced in place.
/assets/*
  Cache-Control: public, max-age=604800

# /go is an ad landing hop, not a page — keep it out of search results. Pages
# does attach this to the 302 itself (verified against the live domain), so the
# hop is marked noindex even though it never renders a body.
/go
  X-Robots-Tag: noindex

/go/
  X-Robots-Tag: noindex
